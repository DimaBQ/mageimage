			<% unless post.user_first_name.nil? %>
				<p><%= post.user.first_name + ' ' + post.user.last_name %></p>
				<%= post.body %>
				<%= link_to "Удалить комментарий",[post.account, post] , method: :delete, data: {confirm: "are you confirm?"} %>
				<hr>
			<% end %>